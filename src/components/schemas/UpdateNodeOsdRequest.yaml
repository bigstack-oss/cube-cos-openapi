type: object
required:
- reweight
properties:
  reweight:
    type: number
    description: The reweight value for the OSD. It's only allow to have two decimal
      places with a range of 0.0 to 1.0, for example, 0.5 or 0.75.
