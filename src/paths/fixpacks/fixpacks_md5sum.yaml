post:
  operationId: uploadFixpackMd5Sum
  tags:
  - Fixpacks
  summary: Upload a fixpack MD5 sum
  parameters:
  - $ref: ../../components/parameters/dataCenter.yaml
  requestBody:
    $ref: ../../components/requestBodies/applicationOctetStream.yaml
  responses:
    '200':
      description: Fixpack MD5 uploaded successfully
      content:
        application/json:
          schema:
            type: object
            required:
            - code
            - msg
            - status
            properties:
              code:
                type: integer
                example: 200
              msg:
                type: string
                example: Fixpack MD5 uploaded successfully
              status:
                type: string
                example: ok
    '409':
      description: Conflict
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                example: 409
              msg:
                type: string
                example: there is a fixpack in uploading status, please try again
                  later
              status:
                type: string
                example: conflict
    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                example: 500
              msg:
                type: string
                example: 'failed to upload fixpack md5: internal server error'
              status:
                type: string
                example: internal server error
